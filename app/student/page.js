const Student = () => {
    return (
        <div className=" mx-4">
            Student Page
            The user should be able to select a student from a list of students, Then let them select, update, delete the book for that student.
            Student Name, Student ID Number, Student Email, Student Phone Number
        </div>
    );
}

export default Student;
